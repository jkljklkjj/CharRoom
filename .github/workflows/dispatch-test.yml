name: Dispatch Test

on:
  workflow_dispatch:

jobs:
  test-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: |
          echo "Dispatch test: Hello from GitHub Actions"
